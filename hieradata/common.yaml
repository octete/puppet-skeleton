---

classes:
  - ntp

# When you don't specify the servers, it picks up the defaults for
# each operating system.
#ntp::servers:
#  - 0.pool.ntp.org
#  - 1.pool.ntp.org
#  - 2.pool.ntp.org
#  - 3.pool.ntp.org

ntp::driftfile: /var/lib/ntp/ntp.drift

nginx::manage_repo: false
